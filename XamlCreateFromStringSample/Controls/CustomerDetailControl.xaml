<UserControl
    x:Class="XamlCreateFromStringSample.Controls.CustomerDetailControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

  <StackPanel>
    <TextBox Header="Firstname" Text="{x:Bind Customer.FirstName,Mode=OneWay}" 
             IsReadOnly="True" Margin="10"/>
    <TextBox Header="Lastname" Text="{x:Bind Customer.LastName,Mode=OneWay}" 
             IsReadOnly="True" Margin="10"/>
  </StackPanel>
</UserControl>
